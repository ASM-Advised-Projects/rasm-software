/**
 *
 */


/**
 *
 */
int main(int argc, char **argv)
{
  // first arg: file system root
  // --test ss1:ss2:ss3
  // --no-test ss1:ss2:ss3
  // only interactive, only non-interactive: --active, --non-active
  // print help and exit program: -h
  // verbose output: -v
  // lest options to pass to the lest framework for all test collections: --lest "<lest options>"
}
